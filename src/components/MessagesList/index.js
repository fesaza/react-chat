import React from 'react'
import CardMessage from '../CardMessage';

const MessagesList = () => {
    return (
        <div>
            <CardMessage />
            <CardMessage />
            <CardMessage />
            <CardMessage />
        </div>
    )
}

export default MessagesList
